<Window x:Class="LiveGardenTVPlus.Views.MiniPlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:material="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
        Title="Mini Player" Height="400" Width="550"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        Topmost="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <Border Grid.Row="0" Background="{DynamicResource AccentBrush}" Height="30" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <TextBlock x:Name="ChannelNameText" Text="No channel selected" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="White" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                <Button x:Name="CloseBtn" Content="✕" Width="Auto" Height="30" HorizontalAlignment="Right" Click="CloseBtn_Click" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="16" Cursor="Hand"/>
            </Grid>
        </Border>

        <wv2:WebView2 x:Name="Player" Grid.Row="1"/>

        <!-- Control bar -->
        <Border Grid.Row="2" Background="{DynamicResource ControlBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,1" Height="40">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <!-- CH- -->
                <Button x:Name="ChDownBtn" Width="Auto" Height="30" Margin="5,0" Click="ChDownBtn_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <material:PackIcon Kind="ChevronDown" Width="24" Height="24" Foreground="{DynamicResource ForegroundBrush}"/>
                </Button>
                <!-- Play/Pause -->
                <Button x:Name="PlayPauseBtn" Width="Auto" Height="30" Margin="5,0" Click="PlayPauseBtn_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <material:PackIcon Kind="Play" Width="24" Height="24" Foreground="{DynamicResource ForegroundBrush}"/>
                </Button>
                <!-- Stop -->
                <Button x:Name="StopBtn" Width="Auto" Height="30" Margin="5,0" Click="StopBtn_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <material:PackIcon Kind="Stop" Width="24" Height="24" Foreground="{DynamicResource ForegroundBrush}"/>
                </Button>
                <!-- Fullscreen -->
                <Button x:Name="FullscreenBtn" Width="Auto" Height="30" Margin="5,0" Click="FullscreenBtn_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <material:PackIcon Kind="Fullscreen" Width="24" Height="24" Foreground="{DynamicResource ForegroundBrush}"/>
                </Button>
                <!-- CH+ -->
                <Button x:Name="ChUpBtn" Width="Auto" Height="30" Margin="5,0" Click="ChUpBtn_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <material:PackIcon Kind="ChevronUp" Width="24" Height="24" Foreground="{DynamicResource ForegroundBrush}"/>
                </Button>
            </StackPanel>
        </Border>

        <!-- Status bar -->
        <StatusBar Grid.Row="3" Background="{DynamicResource ControlBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready" Foreground="{DynamicResource ForegroundBrush}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>