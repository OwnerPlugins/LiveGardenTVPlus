<Window x:Class="LiveGardenTVPlus.Views.ComparePlaylistsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:material="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
        Title="Compare Playlists by Lululla" Height="Auto" Width="Auto"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top toolbar -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <Button x:Name="LoadSecondFileBtn" Content="Load from file..." Click="LoadSecondFileBtn_Click" Width="120" Margin="0,0,5,0"/>
            <Button x:Name="LoadSecondUrlBtn" Content="Load from URL..." Click="LoadSecondUrlBtn_Click" Width="120" Margin="0,0,5,0"/>
            <ComboBox x:Name="CompareFieldCombo" Width="140" Margin="5,0" SelectedIndex="0" SelectionChanged="CompareFieldCombo_SelectionChanged">
                <ComboBoxItem Content="Smart (Priority)" Tag="Priority" IsSelected="True"/>
                <ComboBoxItem Content="Name" Tag="Name"/>
                <ComboBoxItem Content="URL" Tag="Url"/>
                <ComboBoxItem Content="TvgId" Tag="TvgId"/>
                <ComboBoxItem Content="Logo" Tag="Logo"/>
                <ComboBoxItem Content="Group" Tag="Group"/>
                <ComboBoxItem Content="Country" Tag="Country"/>
                <ComboBoxItem Content="Languages" Tag="Languages"/>
                <ComboBoxItem Content="Nanoid" Tag="Nanoid"/>
            </ComboBox>
            <Button x:Name="CompareBtn" Content="Compare" Click="CompareBtn_Click" Width="90" Margin="5,0"/>
            <Button x:Name="ExportMissingBtn" Content="Export missing" Click="ExportMissingBtn_Click" Width="110" Margin="5,0" IsEnabled="False"/>
            <Button x:Name="ExportAllBtn" Content="Export all" Click="ExportAllBtn_Click" Width="100" Margin="5,0" IsEnabled="False"/>
            <Button x:Name="ExportSelectedBtn" Content="Export selected" Click="ExportSelectedBtn_Click" Width="120" Margin="5,0" IsEnabled="False"/>
        </StackPanel>

        <!-- Filters (identici all'editor) -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" CornerRadius="4" Padding="5" Margin="0,5,0,5" Background="{DynamicResource ControlBackgroundBrush}">
            <StackPanel>
                <TextBlock x:Name="FilterLabel" Text="Filters" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource ForegroundBrush}" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <WrapPanel HorizontalAlignment="Center">
                    <!-- Channel Info -->
                    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="6" Margin="5,2" Padding="8,4" Background="{DynamicResource ControlBackgroundBrush}">
                        <StackPanel>
                            <TextBlock x:Name="ChannelInfoLabel" Text="Channel Info" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <WrapPanel>
                                <StackPanel Width="150" Margin="2">
                                    <TextBlock x:Name="NameFieldLabel" Text="Name" FontSize="11" Foreground="Gray"/>
                                    <TextBox x:Name="FilterName" Width="140"/>
                                </StackPanel>
                                <StackPanel Width="150" Margin="2">
                                    <TextBlock x:Name="UrlFieldLabel" Text="URL" FontSize="11" Foreground="Gray"/>
                                    <TextBox x:Name="FilterUrl" Width="140"/>
                                </StackPanel>
                                <StackPanel Width="150" Margin="2">
                                    <TextBlock x:Name="GroupFieldLabel" Text="Group" FontSize="11" Foreground="Gray"/>
                                    <TextBox x:Name="FilterGroup" Width="140"/>
                                </StackPanel>
                                <StackPanel Width="150" Margin="2">
                                    <TextBlock x:Name="LogoFieldLabel" Text="Logo URL" FontSize="11" Foreground="Gray"/>
                                    <TextBox x:Name="FilterLogo" Width="140"/>
                                </StackPanel>
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                    <!-- EPG & Favorites -->
                    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="6" Margin="5,2" Padding="8,4" Background="{DynamicResource ControlBackgroundBrush}">
                        <StackPanel>
                            <TextBlock x:Name="EpgFavLabel" Text="EPG &amp; Favorites" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <WrapPanel>
                                <StackPanel Width="150" Margin="2">
                                    <TextBlock x:Name="TvgIdFieldLabel" Text="TvgId" FontSize="11" Foreground="Gray"/>
                                    <TextBox x:Name="FilterTvgId" Width="140"/>
                                </StackPanel>
                                <StackPanel Width="150" Margin="2" VerticalAlignment="Center">
                                    <CheckBox x:Name="FilterFavorite" Content="Favorite" VerticalAlignment="Center" Margin="0,15,0,0"/>
                                </StackPanel>
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                    <!-- Geo & Country -->
                    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="6" Margin="5,2" Padding="8,4" Background="{DynamicResource ControlBackgroundBrush}">
                        <StackPanel>
                            <TextBlock x:Name="GeoCountryLabel" Text="Geo &amp; Country" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <WrapPanel>
                                <StackPanel Width="120" Margin="2">
                                    <TextBlock x:Name="CountryFieldLabel" Text="Country" FontSize="11" Foreground="Gray"/>
                                    <TextBox x:Name="FilterCountry" Width="140"/>
                                </StackPanel>
                                <StackPanel Width="120" Margin="2" VerticalAlignment="Center">
                                    <CheckBox x:Name="FilterGeoBlocked" Content="GeoBlocked" Margin="0,15,0,0"/>
                                </StackPanel>
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                    <!-- Advanced IDs -->
                    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="6" Margin="5,2" Padding="8,4" Background="{DynamicResource ControlBackgroundBrush}">
                        <StackPanel>
                            <TextBlock x:Name="AdvancedIdsLabel" Text="Advanced IDs" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <WrapPanel>
                                <StackPanel Width="150" Margin="2">
                                    <TextBlock x:Name="NanoidFieldLabel" Text="Nanoid" FontSize="11" Foreground="Gray"/>
                                    <TextBox x:Name="FilterNanoid" Width="140"/>
                                </StackPanel>
                                <StackPanel Width="150" Margin="2">
                                    <TextBlock x:Name="LanguagesFieldLabel" Text="Languages (comma)" FontSize="11" Foreground="Gray"/>
                                    <TextBox x:Name="FilterLanguages" Width="140"/>
                                </StackPanel>
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                    <!-- URLs & Status -->
                    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="6" Margin="5,2" Padding="8,4" Background="{DynamicResource ControlBackgroundBrush}">
                        <StackPanel>
                            <TextBlock x:Name="UrlsStatusLabel" Text="URLs &amp; Status" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <WrapPanel>
                                <StackPanel Width="150" Margin="2">
                                    <TextBlock x:Name="YoutubeFieldLabel" Text="Youtube URL" FontSize="11" Foreground="Gray"/>
                                    <TextBox x:Name="FilterYoutube" Width="140"/>
                                </StackPanel>
                                <StackPanel Width="150" Margin="2">
                                    <TextBlock x:Name="StreamFieldLabel" Text="Stream URL" FontSize="11" Foreground="Gray"/>
                                    <TextBox x:Name="FilterStream" Width="140"/>
                                </StackPanel>
                                <StackPanel Width="150" Margin="2">
                                    <TextBlock x:Name="StatusFieldLabel" Text="Status" FontSize="11" Foreground="Gray"/>
                                    <TextBox x:Name="FilterStatus" Width="140"/>
                                </StackPanel>
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                </WrapPanel>
                <WrapPanel HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button x:Name="ApplyFilterBtn" Content="Apply" Width="100" Margin="5,0" Click="ApplyFilterBtn_Click"/>
                    <Button x:Name="ClearFilterBtn" Content="Clear" Width="100" Margin="5,0" Click="ClearFilterBtn_Click"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <TextBlock x:Name="StatusText" Grid.Row="2" Margin="0,5,0,10" Foreground="{DynamicResource ForegroundBrush}"/>

        <!-- DataGrid -->
        <DataGrid x:Name="ResultsGrid" Grid.Row="3" AutoGenerateColumns="False" 
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  CanUserSortColumns="True" RowHeight="28"
                  Background="{DynamicResource ControlBackgroundBrush}"
                  Foreground="{DynamicResource ForegroundBrush}"
                  RowBackground="{DynamicResource ControlBackgroundBrush}"
                  AlternatingRowBackground="{DynamicResource AlternateRowBackgroundBrush}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  GridLinesVisibility="Horizontal"
                  VirtualizingPanel.IsVirtualizing="True" 
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  Sorting="ResultsGrid_Sorting"
                  SelectionMode="Extended" SelectionUnit="FullRow"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Auto">
            <DataGrid.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="{DynamicResource AccentColor}"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Channel Name" Binding="{Binding Name}" Width="2*" SortMemberPath="Name"/>
                <DataGridTextColumn Header="URL" Binding="{Binding PrimaryUrl}" Width="3*" SortMemberPath="PrimaryUrl"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="Auto" FontWeight="Bold" SortMemberPath="Status"/>
                <DataGridTextColumn Header="Group" Binding="{Binding Group}" Width="Auto" SortMemberPath="Group"/>
                <DataGridTextColumn Header="TvgId" Binding="{Binding TvgId}" Width="Auto" SortMemberPath="TvgId"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="CloseBtn" Content="Close" Click="CloseBtn_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>