<Window
        x:Class="LiveGardenTVPlus.Views.LogoPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:LiveGardenTVPlus.Converters"
        Title="Select Logo by Lululla" Height="Auto" Width="Auto"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="SearchBox" Width="300" ToolTip="Search by logo name"/>
            <Button x:Name="SearchButton" Content="Search" Margin="5,0,0,0" Click="SearchButton_Click" Width="80"/>
        </StackPanel>
        <CheckBox x:Name="ShowThumbnailsCheckBox" Grid.Row="1" Content="Show thumbnails (slower)" Margin="0,0,0,10" Checked="ThumbnailsOptionChanged" Unchecked="ThumbnailsOptionChanged" IsChecked="True"/>
        <ListBox x:Name="LogosListBox" Grid.Row="2" VirtualizingPanel.ScrollUnit="Pixel" VirtualizingPanel.VirtualizationMode="Recycling" SelectionMode="Single" MouseDoubleClick="LogoItem_DoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="5" CornerRadius="6" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <Image Width="40" Height="40" Stretch="Uniform" Margin="0,0,10,0" Source="{Binding Thumbnail}" Visibility="{Binding ElementName=ShowThumbnailsCheckBox, Path=IsChecked, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBlock Text="{Binding Name}" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <Button Grid.Row="3" Content="Cancel" Click="CancelBtn_Click" Width="80" HorizontalAlignment="Right" Margin="0,10,0,0"/>
    </Grid>
</Window>