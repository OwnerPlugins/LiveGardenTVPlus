<Window
        x:Class="LiveGardenTVPlus.Views.EpgWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TV Guide (EPG) by Lululla" Height="Auto" Width="Auto"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Toolbar -->
        <Border Grid.Row="0" Background="{DynamicResource ControlBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="10,5" Margin="0,0,0,10" CornerRadius="4">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock x:Name="ChannelLabel" Text="Channel:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{DynamicResource ForegroundBrush}"/>
                <ComboBox x:Name="ChannelCombo" Width="250" DisplayMemberPath="DisplayName" Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource ForegroundBrush}" SelectionChanged="ChannelCombo_SelectionChanged"/>
                <Button x:Name="RefreshBtn" Content="Refresh" Margin="10,0,5,0" Click="RefreshBtn_Click"/>
                <Button x:Name="DetailsBtn" Content="Details" Click="DetailsBtn_Click"/>
            </StackPanel>
        </Border>
        <!-- DataGrid -->
        <DataGrid x:Name="ProgramsGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource ForegroundBrush}" RowBackground="{DynamicResource ControlBackgroundBrush}" AlternatingRowBackground="{DynamicResource AlternateRowBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" GridLinesVisibility="Horizontal" MouseDoubleClick="ProgramsGrid_MouseDoubleClick">
            <DataGrid.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="{DynamicResource AccentColor}"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Start (local)" Binding="{Binding StartLocal, StringFormat=HH:mm}" Width="80"/>
                <DataGridTextColumn Header="End (local)" Binding="{Binding StopLocal, StringFormat=HH:mm}" Width="80"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="2*"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="3*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>